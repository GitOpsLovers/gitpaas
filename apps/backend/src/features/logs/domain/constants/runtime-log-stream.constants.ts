/**
 * The number of live streams of the output of a container one user holds open at the same time.
 */
export const RUNTIME_LOG_STREAM_MAX_CONNECTIONS = 5;

/**
 * The number of lines of the history the follower asks the daemon for when it opens the stream of a container.
 */
export const RUNTIME_LOG_STREAM_HISTORY_LINES = 5000;
