/**
 * The number of milliseconds a day holds, used to turn the age in days into a moment.
 */
export const DAY_IN_MILLISECONDS = 24 * 60 * 60 * 1000;

/**
 * The largest number of log entries that one removal touches.
 */
export const LOG_RETENTION_BATCH_SIZE = 500;
